{
  "title": "Job Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "platform": {
      "title": "Job Platform",
      "description": "Which job platform to scrape: 'linkedin' or 'naukri'",
      "type": "string",
      "enum": ["linkedin", "naukri"],
      "default": "linkedin",
      "example": "linkedin"
    },
    "job_title": {
      "title": "Job Title / Keyword",
      "description": "Job title or keywords to search for (e.g., 'React Developer', 'Python Engineer', 'Data Scientist')",
      "type": "string",
      "minLength": 1,
      "maxLength": 100,
      "example": "React Developer"
    },
    "location": {
      "title": "Location",
      "description": "City or region to search in (e.g., 'San Francisco', 'New York', 'Remote', 'India')",
      "type": "string",
      "minLength": 1,
      "maxLength": 100,
      "example": "San Francisco"
    },
    "experience": {
      "title": "Experience Level",
      "description": "Required experience (e.g., '2 years', '5+ years', 'Entry Level'). Leave blank for any level.",
      "type": "string",
      "maxLength": 50,
      "example": "3 years"
    },
    "work_mode": {
      "title": "Work Mode",
      "description": "Type of work arrangement",
      "type": "string",
      "enum": ["remote", "hybrid", "onsite", "any"],
      "default": "any",
      "example": "remote"
    },
    "easy_apply": {
      "title": "Easy Apply Only",
      "description": "LinkedIn only: Return only jobs with easy apply option",
      "type": "boolean",
      "default": false,
      "example": false
    },
    "posted_within": {
      "title": "Posted Within",
      "description": "Job posting recency filter",
      "type": "string",
      "enum": ["day", "week", "month", "any"],
      "default": "any",
      "example": "week"
    },
    "maxResults": {
      "title": "Maximum Results",
      "description": "Maximum number of jobs to return (100 max per run)",
      "type": "integer",
      "minimum": 1,
      "maximum": 100,
      "default": 100,
      "example": 50
    }
  },
  "required": ["platform", "job_title", "location"]
}
